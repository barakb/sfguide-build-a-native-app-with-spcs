login
    docker login <SNOWFLAKE_REPO>

router
cd router
    docker build --platform linux/amd64 -t falkordb_router . 
    cd ..
    docker tag falkordb_router <SNOWFLAKE_REPO>/falkordb_router
	docker push <SNOWFLAKE_REPO>/falkordb_router
    cd falkordb
    docker build --platform linux/amd64 -t falkordb_server .
    cd ..
    docker tag falkordb_server <SNOWFLAKE_REPO>/falkordb_server
	docker push <SNOWFLAKE_REPO>/falkordb_server